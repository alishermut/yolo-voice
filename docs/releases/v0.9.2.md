# YOLO Voice v0.9.2: Refinement Update

## What's new

- Ships the latest incremental polish across the current `0.9` release line.
- Packages the recent transcription, settings, and UI work into a fresh installer and updater build.
- Keeps the release cadence lightweight with a patch bump instead of rolling into a larger version reset.

## Why this release matters

- Gives users a clean new build for the latest app improvements without changing the broader `0.9` track.
- Keeps auto-update metadata and GitHub release naming aligned with the installer version.

## Verification

- Passed `npm run build`.
- Built the Windows Tauri release artifacts for `0.9.2`.
